<?xml version="1.0" encoding="UTF-8"?>
<svg width="420" height="90" viewBox="0 0 420 90" fill="none" xmlns="http://www.w3.org/2000/svg">

  <!-- ── Eyebrow: TALKDESK ── -->
  <text
    x="2"
    y="18"
    font-family="'Inter', 'Helvetica Neue', Arial, sans-serif"
    font-weight="600"
    font-size="9"
    letter-spacing="3"
    fill="white"
    fill-opacity="0.45"
  >TALKDESK</text>

  <!-- ── Revenue (white) ── -->
  <text
    x="0"
    y="66"
    font-family="'Inter', 'Helvetica Neue', Arial, sans-serif"
    font-weight="800"
    font-size="54"
    letter-spacing="-2"
    fill="white"
    fill-opacity="0.95"
  >Revenue</text>

  <!-- ── IQ (lilac accent) ── -->
  <text
    x="328"
    y="66"
    font-family="'Inter', 'Helvetica Neue', Arial, sans-serif"
    font-weight="800"
    font-size="54"
    letter-spacing="-2"
    fill="#BF8FE0"
  >IQ</text>

  <!-- ── Hairline ── -->
  <line x1="0" y1="74" x2="418" y2="74" stroke="white" stroke-opacity="0.1" stroke-width="0.75"/>

  <!-- ── Tagline ── -->
  <text
    x="2"
    y="87"
    font-family="'Inter', 'Helvetica Neue', Arial, sans-serif"
    font-weight="500"
    font-size="9"
    letter-spacing="2.8"
    fill="white"
    fill-opacity="0.35"
  >REVENUE INTELLIGENCE PLATFORM</text>

  <!-- ── Lilac dot ── -->
  <circle cx="416" cy="87" r="2.8" fill="#BF8FE0" fill-opacity="0.6"/>

</svg>
